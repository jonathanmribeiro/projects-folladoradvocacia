import 'package:flutter/material.dart';

class SliderSession extends StatelessWidget {
  const SliderSession({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 472,
      child: Center(
        child: Text("slider"),
      ),
    );
  }
}
